node_modules
.next
.env.local
.env*.local
*.log
*.tsbuildinfo
.DS_Store

# Internal AI-process notes — kept locally, never committed to the public repo
CLAUDE.md
docs/agent-memory/
docs/AUDIT-HANDOFF.md
docs/SESSION-HANDOFF.md
docs/task-brief-template.md
.claude/

# Estimator internals — the kickoff doc and golden test carry real business
# numbers and a customer address; they stay local (tests still run locally).
docs/ESTIMATOR_INTEGRATION_KICKOFF.md
lib/estimator/golden-riverton.test.ts

# Perf report carries tenant/business numbers — local-only.
PERF_PHASE0_REPORT.md

# The smoke suite in scripts/perf-investigation/ IS tracked: it encodes
# safety-critical rules (captured-id cleanup, skip-is-never-a-pass) that are
# worth reviewing in diffs. Only its machine- and business-specific values stay
# out — those live in the gitignored config below, and credentials stay in
# .env.local as always.
scripts/perf-investigation/smoke.config.json
# A spent one-off holding production storage-object paths.
scripts/perf-investigation/orphan-thumb-cleanup.mjs
